kind: Service
apiVersion: v1
metadata:
# set your user name
  name: thynder-hello-world
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
# set your user name
    app: thynder-hello-world